import { DefaultTheme } from "styled-components";

const breakpoints = {
  tablet: "768px",
  desktop: "1200px",
};

const fontFamily = {
  main: "Poppins, sans-serif",
};

export const theme: DefaultTheme = {
  breakpoints,
  fontFamily,
};
